import { describe, it } from "vitest";

describe("Navigation", () => {
    it("should render hamburger icon when screen size is small", () => {
        throw Error("unimplemented");
    });

    it("should render nav items and 'x' when hamburger icon is clicked", () => {
        throw Error("unimplemented");
    });

    it("it should hide nave menu items when 'x' is clicked", () => {
        throw Error("unimplemented");
    });
});
